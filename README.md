# Snowball.io — Офисные снежки (WebSocket)

Простая мультиплеерная браузерная игра: **Office A vs Office B**.  
Вся логика на сервере, клиент не может “читерить”.

## Правила

- 2 команды: **Office A** и **Office B**
- У каждой команды:
  - **10 HP**
  - **20 снежков**
- Каждый игрок может сделать **1 действие раз в 5 секунд**:
  - ❄️ бросить снежок
  - 🛡️ включить щит
- Бросок:
  - −1 снежок из запаса команды
  - попадает в случайного **подключенного** игрока вражеской команды
  - если у врага активен щит — урон не проходит, щит теряет 1 прочность
  - если щита нет — враг теряет 1 HP
- Щит:
  - командный (один на всех)
  - **3 прочности**
  - **15 секунд** или пока не сломается
  - блокирует все атаки
- Игра заканчивается, когда HP одной из команд становится 0.

## Запуск локально

1) Установи зависимости:

```bash
npm i
```

2) Запусти сервер:

```bash
npm start
```

3) Открой в браузере:

- `http://localhost:3000`

## Как играть внутри офиса (локальная сеть / LAN)

- Запусти сервер на одном компьютере в офисе.
- Сервер при старте выводит **все доступные LAN-ссылки** вида `http://192.168.x.x:3000`.
- Коллеги в той же Wi‑Fi/сети открывают эту ссылку в браузере.
- Вводят **инвайт‑код из 6 цифр** (например, `123456`) и выбирают разные команды.
- Код придумываешь ты (любые 6 цифр), и сообщаешь коллегам.

### Если коллеги не могут зайти

- Проверь, что они в **той же сети**.
- Разреши входящие подключения для Node/порта `3000` в Windows Firewall.
- Можно сменить порт:

```bash
set PORT=8080
npm start
```

## Удобства

- Кнопка **“Скопировать ссылку”** — шарит URL с `?room=...`
- Горячие клавиши: `1` — бросок, `2` — щит, `R` — новая игра

## Деплой на Render

В репозитории уже есть `render.yaml`, так что самый простой путь:

- Залей код на GitHub
- В Render: **New → Blueprint** и выбери репозиторий
- Render сам подхватит `render.yaml` и запустит сервис

Важно: Render сам выставляет `PORT`, сервер его использует автоматически.

## Пуш в GitHub (репозиторий уже есть)

Репозиторий: [sinnerdys/snowballgame](https://github.com/sinnerdys/snowballgame)

Команды из корня проекта: 

```bash
git init
git add .
git commit -m "Init Snowball office game"
git branch -M main
git remote add origin https://github.com/sinnerdys/snowballgame.git
git push -u origin main
```


